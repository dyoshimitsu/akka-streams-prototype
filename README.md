# akka-streams-prototype

## StreamApplication

```shell
cd play-scala-seed
sbt "runMain StreamApplication"
```

## WebSocketController/connect

```shell
cd play-scala-seed
sbt run
wscat --connect 'ws://localhost:9000/websocket/connect'
```
